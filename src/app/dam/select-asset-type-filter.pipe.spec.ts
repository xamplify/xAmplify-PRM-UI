import { SelectAssetTypeFilterPipe } from './select-asset-type-filter.pipe';

describe('SelectAssetTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectAssetTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
